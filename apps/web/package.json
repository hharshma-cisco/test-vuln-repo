{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "Test fixture \u2014 npm sub-project inside a monorepo. Contains a direct-dep vuln (lodash) and a transitive-dep vuln (qs pulled by express) so both remediation paths are exercised.",
  "scripts": {
    "test": "node --test index.test.js"
  },
  "dependencies": {
    "express": "^5.0.0",
    "lodash": "4.18.0"
  }
}
